{
  "dependencies": {
    "faker": "^4.1.0",
    "jest": "^23.6.0",
    "puppeteer": "^1.9.0"
  },
  "jest": {
    "verbose": true,
    "preset": "./jest.config.js"
  },
  "scripts": {
    "test": "jest",
    "runinband": "jest --runInBand tests/e2e",
    "debug": "NODE_ENV=debug jest",
    "debug-runinband": "NODE_ENV=debug jest --runInBand tests/e2e"
  }
}
