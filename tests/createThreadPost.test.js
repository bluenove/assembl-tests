const data = require("../data");
const signIn = require("./signin.test");

describe("Create a post on a thread", () => {
  beforeEach(() => {
    signIn();
  });
  test("I can post on a thread", () => {});
});
